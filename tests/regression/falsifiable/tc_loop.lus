type nat = subrange [0, *] of int;

type increasing = subtype { n: nat | true -> n > pre n };

type bounded_ty =
  subtype { n: increasing | true -> n - pre n <= 3 };

node I(b:bool) returns (o: bounded_ty);
let
  o = any { n: increasing | true  };
tel

